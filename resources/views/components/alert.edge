<div {{ $props.id ? `id=${$props.id}` : '' }} class="{{
  clsx('border-2 px-2 py-3', {
    'border-red-500 bg-red-100 text-red-500': ($props.type ?? 'error') === 'error',
    'border-teal-700 bg-teal-50 text-teal-700': $props.type === 'success',
  })
}}" role="alert" aria-live="assertive">
  {{{ await $slots.main() }}}
</div>
